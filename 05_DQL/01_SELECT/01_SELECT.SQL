-- IT IS A STATEMENT WHICH IS USED TO RETRIVE THE DATA FROM THE DATABASE AND DISPLAY IT

-- SELECT COMES AT THE END OF THE ORDER OF EXECUTION

ASTERISK(*)(CALL)
SEMI_COLON(I)(TERMINATE)
COMMA(,)(FOR PASSING MULTIPE COLUMNS)

2 SELECT COL_NAME
1 FROM TABLE_NAME;

SELECT *
SELECT EMP.*
SELECT ENAME
SELECT ENAME, SAL
SELECT ENAME, SAL, SAL*6
SELECT ENAME, SAL*12, DEPTNO, MGR, COMM 
SELECT ENAME, SAL, HIREDATE, JOB etc.. etc..
SELECT COUT(*), MAX(ENAME), MIN(ENAME)
SELECT COUT(*), SUM(SAL), AVG(SAL)

FROM ENP; 